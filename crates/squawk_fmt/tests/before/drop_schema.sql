drop schema analytics;

drop schema if exists extraordinarily_long_schema_name_for_customer_reporting_and_business_intelligence, another_extraordinarily_long_schema_name_for_historical_application_analytics cascade;

-- comments in every position
drop /* schema */ schema /* if */ if /* exists */ exists /* first schema */ analytics /* comma */, /* second schema */ reporting /* behavior */ restrict /* end */;
