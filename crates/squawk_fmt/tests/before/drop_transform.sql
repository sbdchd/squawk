drop transform for public.customer_document language plpgsql;

drop transform if exists for extraordinarily_long_schema_name.extraordinarily_long_customer_document_type_name language extraordinarily_long_procedural_language_name cascade;

-- comments in every position
drop /* transform */ transform /* if */ if /* exists */ exists /* for */ for /* type */ public /* dot */ . customer_document /* language */ language /* language name */ plpgsql /* behavior */ restrict /* end */;
